# TrustReconciler — environment configuration template.
# Copy this file to `.env` and fill in your own value. `.env` is
# git-ignored and MUST never be committed.
#
# Backend scripts read the key via os.environ["ETHERSCAN_API_KEY"]
# (see scripts/etherscan_config.py). Never hardcode a real key in
# any .py/.ts file, commit message, or documentation.

ETHERSCAN_API_KEY=
